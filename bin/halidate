#! /usr/bin/env ruby

require_relative '../lib/halidator'

input = ARGF.read

hal = Halidator.new(input)
res = hal.valid?
if true == res
  $stdout.puts "true"
else
  $stdout.puts 'false'
  $stderr.puts hal.errors
end
